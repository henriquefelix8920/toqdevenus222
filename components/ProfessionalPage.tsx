'use client'; import Image from 'next/image'; import Link from 'next/link'; import {useState} from 'react'; import type {Professional} from '@/data/professionals';
export default function ProfessionalPage({p}:{p:Professional}){const [selected,setSelected]=useState<string|null>(null);return <main className="profile"><section className="profileHero"><Image src={p.images[0]} alt={`Fotografia de ${p.name}`} fill priority sizes="100vw"/><div className="heroShade"/><div className="profileTitle"><p className="eyebrow">TOQ DE VENUS · PRESENÇA 01</p><h1>{p.name}</h1></div></section><section className="profileContent"><p className="eyebrow">INTRODUÇÃO</p><h2>{p.bio}</h2><div className="profileExperience"><span>EXPERIÊNCIAS</span>{p.experiences.map((x,i)=><p key={i}>{x}</p>)}</div><div className="profileGallery">{p.images.map((src,i)=><button key={src} onClick={()=>setSelected(src)} aria-label={`Ampliar fotografia ${i+1}`}><Image src={src} alt={`Fotografia ${i+1} de ${p.name}`} fill sizes="(max-width:768px) 90vw, 40vw"/></button>)}</div><div className="profileCTA">{p.whatsappNumber?<a className="cta" href={`https://wa.me/${p.whatsappNumber}`}>AGENDAR COM {p.name.toUpperCase()} ↗</a>:<span className="cta disabled">WHATSAPP EM CONFIGURAÇÃO</span>}<Link href="/" className="back">← VOLTAR</Link></div></section>{selected&&<button className="lightbox" onClick={()=>setSelected(null)} aria-label="Fechar imagem"><Image src={selected} alt="Imagem ampliada" fill sizes="100vw"/></button>}</main>}
