import Link from 'next/link'; import Image from 'next/image'; import {professionals} from '@/data/professionals';
export default function ProfessionalsSection(){return <section id="profissionais" className="professionals"><div className="sectionIntro"><p className="eyebrow">AS CINCO PRESENÇAS</p><h2>Cada nome é<br/><i>uma porta.</i></h2></div><div className="people">{professionals.map((p,i)=><Link className="person" href={'/'+p.slug} key={p.slug}><span className="num">0{i+1}</span><div className="personImage"><Image src={p.images[0]} alt={`Fotografia de ${p.name}`} fill sizes="(max-width:768px) 90vw, 48vw"/></div><h3>{p.name}</h3><span className="discover">CONHECER ↗</span></Link>)}</div></section>}
